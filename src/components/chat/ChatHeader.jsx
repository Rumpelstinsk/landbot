/**
 * ChatHeader: Component to display chat message header
 * @component
 * @returns {JSX.Element}
 */
export const ChatHeader = () => (
    <div className="landbot-header">
      <h1 className="subtitle">Landbot</h1>
    </div>
)